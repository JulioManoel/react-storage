import { View } from "react-native"

export default ItemTab = () => {
  return (
    <View style={{ justifyContent: 'center', flexDirection: 'row'}}>
      <View style={{ height: 1, width: "80%", backgroundColor: "#989898" }} />
    </View>
  )
}